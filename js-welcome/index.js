/**
 * Возвращает x в степени n
 * 
 * @param {number} x число, которое необходимо поднести к степеню
 * @param {number} n степень, в которую нужно поднести
 * @return {number}  x поднесённый в n-ную степень
*/
function pow(x, n) {
    return x ** n;
}