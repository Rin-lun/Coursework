/*
for

for(начальное состояние; условие, за которым цикл повторяется; изменение счётчика на каждой итерации) {
    блок кода, который повторяется
}

Начальное состояние - начальное состояние для счётчика

*/

// Задача: Вывести 10 консоль.логов

for(let i = 0; i < 0; i++) {
    console.log(i);
}