/*
Синтаксис if-else:

if(логическая операция, результатом которой будет Boolean) {
    блок кода, который будет выполнятся, если условие = екгу (истина)
} else {
    блок кода, которыйы будет выполнятся, если условие = false (ложь)
}

if - else if - else

Синтаксис

if(условие 1) {
    блок кода, который выполнится, если условие1 = true
} else if {
    блок кода, который выполнится, если условие2 = true
} else {
    блок кода, который выполнится, если условие1 и условие2 = false
}
*/